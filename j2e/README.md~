Projet Cookie
=============

Demonstration code for the "Serveur d'Entreprise" course [2014]

Please use the following command to run the server:

mvn clean package tomee:run 

there is the option -DskipTests fot the distant machine

mvn clean package tomee:run -DskipTests "host"

Then, open http://localhost:8080/




